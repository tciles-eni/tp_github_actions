name: Ma Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    run-on: node:lts-apline3.23
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm test
